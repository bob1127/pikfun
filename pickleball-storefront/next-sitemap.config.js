/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://www.kesh-de1.com', // 這裡換成你的網址
  generateRobotsTxt: true, // 自動幫你生成 robots.txt
  // 如果有不需要收錄的後台頁面，可以在這裡排除
  // exclude: ['/admin/*'], 
}